#!/bin/bash
#PBS -lmem=32gb
#PBS -V

while getopts m:p: option
do
  case "${option}"
    in
      m) metalin=${OPTARG};;
      p) metalloc=${OPTARG};;
    esac
done
metaluse=$(awk lineno=$PBS_ARRAYID '{if (NR == lineno) print }' $metalin )

$metalloc $metalin
